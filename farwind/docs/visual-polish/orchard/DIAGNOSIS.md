# 果园花床与池边长椅：修改前核实

当前分支为 `codex/initial-game`，HEAD 为 `c4c864af96813b6a1386c75ee38e95ca8bd4d65b`。工作区包含上一轮视觉修复和其他任务的未提交改动，未切换、覆盖或提交。完整初始状态及三处源码摘要见 `baseline-state.json`。

左箭头为南侧果园花床。`src/game/systems/terrain.ts` 修改前第136–150行在 `(230,1480,210,80)` 用棕色底矩形、四排直线木条和绿色/粉色方块绘制，缺少土壤、植物轮廓和木板体积；并非贴图加载失败。它仅烘焙在地表，没有独立实体或碰撞。旁边的浆果、宝箱和围栏是另外的对象。

右箭头为池塘西桥南端树后的小木长椅。同文件修改前第218–230行用两条座板矩形和两根立柱绘制，缺少靠背、座面和支架层次。两个相同实例的基准坐标为 `(900,1350)`、`(1500,1320)`，对应落地点中心 `(940,1380)`、`(1540,1350)`。它们仅在地表，不参与碰撞或交互。西椅被 `tree-13` `(980,1540)` 树冠遮挡，东椅被 `tree-12` `(1530,1510)` 树冠遮挡；这属于现有布局，不能把遮挡误判为图片缺失。

修改前已保存正常 HUD 全景和隐藏 HUD 后的纯裁切局部图，视口为1280×800及1365×853。主机位玩家 `(660,1510)`；两个长椅机位分别 `(940,1400)`、`(1570,1400)`，利用现有玩家靠近树冠时的半透明行为查看，不移除树木。截图在 `before/`、`before/west-bench/`、`before/east-bench/`。均通过正式存档导入设置机位，行为验收另使用真实键盘输入。

待验证：生成器是否产生真实透明通道、正常显示大小下植物和座椅结构是否可辨识、按落地点排序后的前后遮挡、附近浆果/宝箱交互、非整数缩放、场景重启及资源开销。花床保留地面装饰语义；长椅使用现有 prop 体系并保留无碰撞装饰语义，避免新增通行阻挡。
