# 修改前基线

- 核对日期：2026-09-26。
- 项目：`farwind/`；分支：`codex/initial-game`。
- 实际提交：`c4c864af96813b6a1386c75ee38e95ca8bd4d65b`。
- 工作树已有战斗、弹反、地图、碰撞、村庄视觉和底部木框对话栏改动；本次保留，不回退远端文件。
- 当前对话接口已有第三个来源参数；居民使用稳定角色标识，但旧未知居民会误映射到木匠。
- 旧人物显示源为 `/assets/{角色标识}.png` 的双帧图集第一帧，旧样式按 128 像素方形裁切；场景同用这些图集。
- 原角色核对：小满棕发髻、绿衣、米白围裙和药篮；岚爷爷白发须、绿披肩、亚麻衣、木杖；阿禾草帽、黑发须、米色袖衫、棕围裙和木工工具。

## 修改前命令

- `npm run typecheck`：失败，`World.ts` 导入的 `parryVisual`、`parryWeapon` 在 `animation.ts` 中未导出。
- `npm test`：84 项中 79 项通过，5 项失败。`obstacles.test.ts` 四项敌人绕障、前摇时间单位、返家行为失败；`village-defense.test.ts` 一项仍期待旧布尔返回值，当前返回接触事件对象。
- 这些问题在本任务修改前出现，属于已有战斗工作；不删除断言、不跳过、不通过修改战斗语义取得通过。

旧界面参考保留于 `docs/dialog-design/`。后续报告将区分素材固定预览、真实游戏运行、工程检查和用户美术验收。
