# 提交前检查

- 本次逻辑测试：54项通过；类型检查及生产构建通过。
- 本次默认三进程浏览器回归：17项通过、1项失败。两项训练稻草人用例均通过。
- 失败位置：`tests/drop.spec.ts:47`；固定节奏攻击后未击杀`leaf-1`，等待5秒超时，击杀列表仍为`slime-1`、`slime-2`。
- 与`REPORT.md`记录的旧满包并行失败一致；根因未完全确认，没有将本次运行标记为全绿，也没有重复运行掩盖失败。
- 本次失败截图、错误上下文和追踪包保留在本地被忽略的`test-results`目录。
- `evidence/corner-failure-trace.zip`约236MB，超过远端普通Git单文件限制，保留本地并加入忽略规则；其余代码、素材与证据纳入提交。
- 回归测试重新生成的媒体、截图和存档恢复为测试前已暂存版本，以保留原报告中的证据及摘要对应关系。
